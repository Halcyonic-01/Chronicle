apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: victim-apps
  namespace: default
  labels:
    release: monitoring
spec:
  selector:
    matchExpressions:
      - key: app
        operator: In
        values: ["frontend", "api", "worker"]
  endpoints:
  - port: http
    path: /metrics
